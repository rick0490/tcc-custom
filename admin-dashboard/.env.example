# TCC Custom - Admin Dashboard Environment Variables
# Copy this file to .env and fill in the values

# Server Configuration
PORT=3000
NODE_ENV=production

# Anthropic API Key (for AI seeding and tournament narratives)
# Get your key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Session Secret (generate a random string)
SESSION_SECRET=your_random_session_secret_here

# Optional: Web Push VAPID Keys (for push notifications)
# Generate with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_EMAIL=mailto:your_email@example.com

# File Paths
FLYERS_PATH=/root/tcc-custom/admin-dashboard/flyers
MATCH_STATE_FILE=/root/tcc-custom/admin-dashboard/tournament-state.json

# Display Service URLs (bracket-display uses WebSocket, no state file needed)
# BRACKET_API_URL defaults to http://localhost:2053
