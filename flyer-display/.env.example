# Flyer Display Service Configuration

# Server port (uses legacy MagicMirror-flyer port for admin-dashboard compatibility)
PORT=2054

# Admin Dashboard URL (for flyer image fetching)
ADMIN_DASHBOARD_URL=http://localhost:3000

# Admin Dashboard WebSocket URL (for real-time updates from browser)
# IMPORTANT: Use internal IP to bypass Nginx Proxy Manager WebSocket issues
# If using NPM with WebSocket support configured, you can use the external URL
ADMIN_WS_URL=http://192.168.1.28:3000

# Enable debug logging
DEBUG_MODE=false
